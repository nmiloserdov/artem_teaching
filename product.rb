module CarApplication
  class Product
    def initialize(brand_name, model, color, price, category)
      @brand_name = brand_name
      @model = model
      @color = color
      @price = price
      @category = category
    end
  end
end
